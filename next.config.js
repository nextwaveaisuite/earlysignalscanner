/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true
  // Note: In Next 14, the App Router is the default; no experimental.appDir needed.
};

module.exports = nextConfig;
